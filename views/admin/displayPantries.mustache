<html>
    <head>
        {{>header}}
        <link rel="stylesheet" href="/css/displayPantries.css">
    </head>
    <body>
        {{>adminNav}}
        <h1 class="title mt-5 mb-4 text-white">Pantries</h1>
        {{#pantry}}
            <div class="container mb-4">
                <div class="card">
                    <div class="card-body">
                    <form action="/deletePantry" method="POST">
                        <input type="hidden" name="pantryId" value="{{_id}}">
                        <input type="hidden" name="pantry" value="{{user}}">
                        <input type="hidden" name="location" value="{{location}}">
                        <h2 class="card-title">Pantry: {{ pantry }}</h2>
                        <h2 class="card-title">Location: {{ location }}</h2>
                        <button type="submit" class="btn btn-primary delete">Delete</a>
                    </form>
                </div>
            </div>
        </div>
        {{/pantry}}
        {{^pantry}}
            <div class="container mb-4">
                <h1 class="mt-5 mb-4">Pantry Information</h1>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">No pantries currently available</h5>
                    </div>
                </div>
            </div>
        {{/pantry}}
        <div class="center">
            <a href="/createPantryPage" class="btn btn-primary">Create Pantry</a>
        </div>
        {{>footer}}
    </body>
</html>
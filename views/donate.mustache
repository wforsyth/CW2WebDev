<!-- donate.mustache -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Donation Form</title>
    <link rel="stylesheet" href="/css/login.css">
    {{>header}}
</head>
<body>
    {{>navbar}}

    <style>
        body{
            background-color: #3a5f5e;
        }
        .btn{
            margin-bottom: 0.1in;
        }
        .expirationDate1{
            margin-bottom: 0.1in;
        }
    </style>

    <div class="container center-content">
        <div class="row justify-content-center form-content">
            <div class="col-md-6">
                <h1 class="text-center">Food Donation Form</h1>
                <div id="formContent">
                    <form action="/donate" method="POST">
                        <!-- Food Item Input Fields -->
                        <div id="foodItems">
                            <div class="food-item">
                                <label for="foodName1">Food Name:</label>
                                <input type="text" id="foodName1" name="foodName[]" class="form-control" required>

                                <label for="quantity1">Quantity:</label>
                                <input type="number" id="quantity1" name="quantity[]" class="form-control" required>

                                <label for="expirationDate1">Expiration Date:</label>
                                <input type="date" id="expirationDate1" name="expirationDate[]" class="form-control" required>
                            </div>
                        </div>

                        <!-- Add Another Food Item Button -->
                        <button type="button" id="addFoodItem" class="btn btn-secondary btn-block">Add Another Food Item</button>

                        <!-- Submit Button -->
                        <button type="submit" class="btn btn-primary btn-block">Donate</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>